name: Keep Render Awake

on:
  schedule:
    - cron: "*/9 * * * *" # Runs every 14 minutes
  workflow_dispatch: # Allows manual triggering from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Portfolio Website
        run: curl -s https://dimas-sp.me/ > /dev/null
